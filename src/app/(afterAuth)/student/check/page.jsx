import StudentCheck from '@/container/StudentCheck';

export const metadata = {
    title: '출결현황',
    description: '출결현황',
}

const StudentCheckPage = () => {
    return <StudentCheck />;
}

export default StudentCheckPage;